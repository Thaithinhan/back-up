const express = require('express');
const { getOauthGoogle } = require('../controllers/oauthGoogleController');
const router = express.Router();

// router.post('/', getOauthGoogle);

module.exports = router;
